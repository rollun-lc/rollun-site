.claude
.agents
.idea
.env
.env.local

# Node / Next.js build artifacts
node_modules/
.next/
/build
out/
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
.DS_Store

# Payload user uploads (Media staticDir) — keep the dir, ignore the files
/public/media/*
!/public/media/.gitkeep

# Large binary archives (exceed GitHub 100MB limit) — keep out of git
*.zip
.bmad-loop/runs/
.bmad-loop/cache/

# Extracted design reference (from gitignored zip)
.design/
