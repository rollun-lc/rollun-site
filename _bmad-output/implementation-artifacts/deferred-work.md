# Deferred Work

Items surfaced incidentally during review, deferred for later focused attention.

- source_spec: `spec-1-2-design-system-tokens-fonts-buttons.md`
  summary: `src/app/(site)/layout.tsx` sets `<html lang="ru">` but the site content is English (all prototypes EN, US B2B, Epic 2 form English) — should be `lang="en"`.
  evidence: Pre-existing from Story 1.1 scaffold; surfaced by review's Cyrillic-subset findings. Latin-only font subsets are correct for English content; the `lang` attribute is a separate a11y/SEO correctness bug, out of scope for the DS-tokens story.

- source_spec: `spec-1-3-header-desktop-mobile.md`
  summary: No "skip to content" bypass link before the persistent site nav — keyboard users must tab through all 6 nav links on every one of the 6 routes (WCAG 2.4.1 Bypass Blocks, level A).
  evidence: Real, but cross-cutting: a proper skip link needs a stable `<main id>` landmark target and a shell-level decision on page/main structure (the layout renders `{children}` directly, pages own their own markup). Not local to the header diff — belongs to a focused shell/a11y pass, not a header follow-up patch.

- source_spec: `spec-1-3-header-desktop-mobile.md`
  summary: No `prefers-reduced-motion` handling for the header's scroll-reactive motion (height shrink + `translateY(-100%)` hide slide + box-shadow); WCAG 2.3.3 vestibular concern.
  evidence: Real, but better solved DS-wide than header-only: Story 1.5 adds reveal-on-scroll (more motion needing the same treatment) and theme.css already tokenizes motion. A header-scoped patch would be a fragmented half-measure; warrants one DS-level `@media (prefers-reduced-motion: reduce)` policy pass. Prototype has none, so this is a deliberate a11y enhancement beyond faithful reproduction.

- source_spec: `spec-1-4-footer-desktop-columns-mobile-accordions.md`
  summary: Mobile footer accordion open-state uses a verbatim fixed `max-height:420px` (shell.css `.site-footer--mb .facc.open .facc-body`); content taller than 420px (browser text-zoom, translated/i18n strings, or editable `SiteSettings` data in Epic 7) is silently clipped and its links become unreachable.
  evidence: Faithful to the prototype now (AD-13; `mobile.css:135`) and under the ceiling for current static content (~200px), so not a defect in this story. Becomes real when footer content is no longer fixed — most likely at the Epic 7 `SiteSettings` seam or under i18n. The robust replacement is animating `grid-template-rows:0fr↔1fr` (or measuring `scrollHeight`) instead of a fixed clamp; deferred because changing it now would deviate from the pixel source of truth with no present benefit.

- source_spec: `spec-1-5-mobile-chassis-drawer-scroll-lock-reveal.md`
  summary: The mobile nav drawer has no modal focus management — opening it does not move focus into the drawer, closing does not restore focus to the burger, Tab is not trapped inside the open drawer, and it lacks `role="dialog"`/`aria-modal`. Keyboard/SR users can tab out of the open drawer into the scrim-covered background.
  evidence: Real a11y gap, but beyond the prototype (plain `mobile.js` has zero focus handling) and beyond this story's spec. `inert`/`aria-hidden` on the closed drawer and Escape-to-close are already implemented; a full focus-trap + dialog semantics + focus restore is a deliberate enhancement that deserves focused implementation and testing (initial-focus target, trap edges, restore on all four close paths) rather than an unattended patch. Applies equally to any future modal overlay, so warrants one shared a11y-modal approach.

- source_spec: `spec-1-6-pixel-acceptance-breakpoint-checklist-preview.md`
  summary: Catalog's `981` breakpoint is a hover-gated query (`@media (hover:hover) and (min-width:981px)`), paired with `@media (hover:none)` and `prefers-reduced-motion:reduce` branches; the pixel checklist covers width-bands (validated with a mouse/hover pointer) but has no acceptance rows for the touch-at-desktop-width (hover:none) layout or the reduced-motion catalog rendering.
  evidence: Real latent coverage gap surfaced by the edge-case review, but non-blocking here: this story only fixes the pixel-band acceptance contract, and the Catalog page itself does not exist yet (built in Epic 5). Hover/pointer-capability and reduced-motion acceptance belong with the Catalog implementation story, where the interaction branches actually ship and can be verified. Width-band math for /catalog is correct as written.

- source_spec: `spec-2-1-contactform-component-validation.md`
  summary: ContactForm uses hardcoded global DOM ids (`contactForm`, `cf-name`, `cf-email`, …) baked into the content module; two simultaneous mounts (Story 2.4 renders the same form as a desktop modal AND in page flow / mobile-nav) will produce duplicate `id`s, breaking `htmlFor` label→input and `aria-describedby` error associations (clicking a label focuses the first matching instance).
  evidence: Real, but latent — Story 2.1 mounts exactly one instance (inline on `/contact`), where the ids are valid and correctly match the pixel prototype's `cf-*` ids. The collision only manifests when Story 2.4 introduces the second concurrent mount. The fix (scope ids with React `useId`) is a small change but belongs with 2.4's multi-mount work, where it can be verified against all three display modes; doing it now would fork the id contract from the prototype with no present consumer.

- source_spec: `spec-2-1-contactform-component-validation.md`
  summary: The client-validation error UX has a11y gaps beyond the prototype — only the focused (first-invalid) field's message is announced via `aria-describedby`; the other N−1 error messages appear silently (no `role="alert"`/live region); required fields carry no `aria-required` or visual required marker; and the in-page mount's outline jumps to `<h3>` with no `<h1>`.
  evidence: Real, but consistent with this project's established pattern of deferring a11y-beyond-prototype to a focused pass (see the header reduced-motion, drawer focus-trap, and skip-link deferrals). The prototype has no validation and no required markers, so these are net-new a11y surface. `role="alert"`/`aria-required` are invisible and cheap but warrant deliberate testing (multi-error announcement order, SR verification) rather than an unattended patch; the `<h1>` outline belongs to the real `/contact` page structure built in Story 2.4, not this bare verification mount.

- source_spec: `spec-2-2-crm-submit-server-action-states.md`
  summary: ContactForm submit SUCCESS is not announced to assistive tech — the error path got `role="alert"`, but success only swaps the submit button's text to `THANK YOU ✓` (a label change inside a button is not reliably announced by screen readers) and focus is never moved, so AT users may not perceive the send succeeded before the 1600ms reset wipes the form.
  evidence: Real, and matches the project's a11y-beyond-prototype deferral pattern (see the 2.1 error-announcement, header reduced-motion, drawer focus-trap deferrals). The prototype (Contact.html) also only swaps the button text — no success live-region — so a `role="status"` success announcement is net-new a11y surface. It is cheap but warrants deliberate SR verification (announcement timing vs the 1600ms reset, whether to move focus) rather than an unattended patch, and pairs naturally with the deferred 2.1 error-UX a11y pass. The core AC (visible confirmation + reset) is met faithfully.

- source_spec: `spec-2-3-anti-spam-protection.md`
  summary: The security-relevant `submitContactForm` logic — the honeypot silent-drop (`{ ok: true }` with no CRM POST) and the 6-field CRM-body stripping (`const { honeypot: _honeypot, ...lead }`) — has no automated test coverage, so a future refactor that reorders the honeypot check after the POST, or drops the key-strip, would ship silently.
  evidence: Real, but pre-existing and project-wide: the project ships no test runner by explicit design (spec Never-boundary — verification is build/lint/manual, same as stories 2.1/2.2), so this is not a defect of this story. `submitContactForm` already takes an injectable `action` and `validateContactForm` is pure, so the drop-path and 6-field-body invariants are trivially unit-testable once a runner is adopted. Adding a runner is forbidden here; belongs to a focused testing-adoption pass, where the honeypot silent-drop and CRM-body contract are prime regression targets.

- source_spec: `spec-2-4-three-form-display-modes.md`
  summary: `ContactModal` renders a `role="dialog"` `aria-modal="true"` with no focus management — focus is not moved into the dialog on open, there is no focus trap, and background content is not `inert`; a static `aria-label="Contact form"` instead of `aria-labelledby` on the form heading.
  evidence: Real dialog-a11y gap (WCAG 2.4.3 / WAI-ARIA dialog pattern), but a11y beyond the ported prototype (Home.html:1457-1476 modal JS has none) and the epic deprioritizes a11y under pixel fidelity (AD-13, epic-2-context). The modal ships unmounted (Epic 3/4 mounts + live-verifies it); a coherent focus-trap+inert+labelledby pass belongs at that mount, not a partial focus-in now (a half-trap is worse than none). Deep-link-open already moves focus to the name field when a topic is supplied.

- source_spec: `spec-2-4-three-form-display-modes.md`
  summary: No `prefers-reduced-motion` guard on the Story 2.4 motion — the deep-link smooth `window.scrollTo` (ContactForm), the `cf-pulse` keyframe, and the overlay opacity + modal `translateY/scale` transitions (components.css) all run unconditionally.
  evidence: Ported verbatim from the prototype (which has no guard); real vestibular concern (WCAG 2.3.3) and consistent with the project's motion stance in later epics (Story 3.2/3.4 ACs call for `prefers-reduced-motion`). Better landed as one coherent DS-level `@media (prefers-reduced-motion: reduce)` policy pass (same as the deferred header/reveal motion items) than a fragmented per-story half-measure.

- source_spec: `spec-2-4-three-form-display-modes.md`
  summary: `ContactForm` hardcodes `id="contactForm"` and fixed field ids (`cf-name`/`cf-topic`/…, from Story 2.1 content); mounting two `GetInTouch` triggers, or a `GetInTouch` alongside the inline `/contact` form, on one page yields duplicate DOM ids → broken `<label htmlFor>` / `aria-describedby` / `getElementById` associations.
  evidence: Pre-existing id design from Story 2.1 (fixed literal ids in contact-form.ts), surfaced — not caused — by Story 2.4 making the form mountable in multiple modes via the drop-anywhere `GetInTouch`. No current page mounts two instances (Contact = inline only; Home/About = one trigger). Becomes real if Epic 3 places multiple triggers; the fix (per-instance id scoping via React `useId`) touches the 2.1 form/content contract and belongs to a focused pass when multi-mount actually lands.

- source_spec: `spec-3-3-product-line-switcher.md`
  summary: Home desktop product-line carousel lacks screen-reader semantics — inactive `.line-slide`s stay in the accessibility tree (hidden only via `opacity:0`, not `aria-hidden`), there is no `role`/`aria-roledescription="carousel"` container, and slide switches are announced by no live region, so AT users perceive all 4 slides at once and no change on switch.
  evidence: Real WCAG concern surfaced by Story 3.3 making the carousel interactive, but the un-hidden-slide structure is pre-existing SSR markup from Story 3.1 (opacity-only visibility, captions/alts always present). Story 3.3 patched the interactive control state (`aria-pressed` on dots) but full carousel semantics (aria-hidden toggling on slides + roledescription + a considered live-region strategy that isn't noisy) is a coherent a11y pattern better landed in one focused pass across the site's carousels than a fragmented per-story half-measure.

- source_spec: `spec-5-1-catalog-scaffold-lines-switching.md`
  summary: Desktop deep-link to `/catalog#automotive` or `#health` (e.g. the Home page's product-line links) reveals the chosen line but does NOT bring it into view — the viewport stays on the hero, so the visitor sees "nothing happened" while the line sits far below the fold.
  evidence: Spec-compliant (the I/O matrix inside the intent-contract mandates `choose(cat, false)` — reveal without scroll — matching the prototype's `choose(cat, false)` on hash). In the flat prototype the browser's native fragment scroll to `id="health"`/`id="automotive"` did the scrolling; the single-DOM port moved those ids onto the (display:none-on-desktop) mobile sections to avoid duplicate ids, so native scroll no longer fires on desktop. Fixing it means the island should scroll on deep-link too — but that contradicts the "без скролла" I/O row, which lives inside `<intent-contract>` and cannot be amended unattended. Needs a human intent decision on whether desktop deep-link should scroll-into-view.

- source_spec: `spec-5-1-catalog-scaffold-lines-switching.md`
  summary: Activating an entrance ("Explore Automotive/Health") on desktop reveals the `.lines-split` section elsewhere in the DOM with no focus management or ARIA — no `aria-expanded`/`aria-controls` on the entrance, no focus move to the revealed line, no live-region announcement; keyboard/screen-reader users get no feedback that content appeared.
  evidence: Real a11y gap on an interactive control this story owns, but a faithful port of the prototype (which also lacks it) and within the epic's stated "Accessibility Floor (basic, not full WCAG AA)" where "при конфликте a11y с пикселем — пиксель приоритетнее". A revealed same-page section is not an overlay (no focus-trap mandated). Deferred to a focused Catalog-a11y pass: move focus to the revealed section (`tabindex=-1` + `focus({preventScroll:true})`) and add `aria-expanded`/`aria-controls` on the entrances, weighed against the pixel-fidelity priority.

- source_spec: `spec-5-3-offers-runtime-buildoffers.md`
  summary: The AD-9 server/client boundary for `src/lib/offers.ts` (the offer-derivation module that must never reach a `'use client'` island) is enforced only by convention and comments — add build-time enforcement via `import 'server-only'` at the top of the module so any accidental client-side runtime import fails the build (the `import type { Offer }` path stays valid for the 5.4 quick-view).
  evidence: Real hardening of this story's headline invariant, but the `server-only` package is not installed and has no precedent in the repo, so applying it means adding a dependency (package.json + lockfile) — a dependency decision beyond an unattended trivial patch. The invariant holds today (verified: no `'use client'` file imports the module; `ProductCard`/`ProductLines` are RSC), so this is preventive, not a current defect. When picked up: `npm i server-only`, add `import 'server-only'` as the first line of `src/lib/offers.ts`, re-run build.

- source_spec: `spec-5-4-product-quick-view.md`
  summary: The quick-view open/close motion (desktop `.pd-panel` `scale(.94)→1`+fade, mobile `.pd-sheet` `translateY(100%)→0`, backdrop fade) never actually animates — the animated elements live inside `.pd-modal`, which flips `display:none↔block` in the same style commit as `.open`, and a CSS transition cannot run out of a `display:none` subtree, so both enter and exit snap instantly to their final frame.
  evidence: Verified faithful to the prototype — `Catalog.html` `openDetail` (~1566) and `Catalog Mobile.html` `openDetail` (~811) do a bare `modal.classList.add('open')` from the same `.pd-modal{display:none}→.open{display:block}` structure with no reflow/rAF two-step — so it is NOT a regression, but the spec's AC ("вход `scale .94→1` / `translateY→0`") and its reduced-motion Design Note both assume the transition plays, and the spec's own residual risk flags motion as not browser-verified. Motion fidelity is the project's #1 priority. A real fix is cross-cutting and needs a design decision (do our overlays animate, or match the prototype's instant snap?): add `@starting-style` + `transition-behavior: allow-discrete` to a shared modal pattern, or a `display`-first-then-rAF `.open` two-step in the island — not an unattended patch, since naively adding it would deviate from the mandated verbatim source. Applies to any future overlay built on this `.pd-modal`/drawer pattern.

- source_spec: `spec-5-4-product-quick-view.md`
  summary: On mobile a horizontal image-swipe on a product card can fall through to the card's `click` handler and accidentally open the quick-view — the Story 5.2 slider wires passive `touchstart/touchmove/touchend` on `.pc-media` (no `preventDefault`, no post-swipe click suppression), and the Story 5.4 card `click→openDetail` guards only `.pc-arrow`, which mobile cards do not render.
  evidence: Confirmed in code (`CatalogCardSlider.client.tsx:81-83` passive touch listeners with a 36px threshold that never suppress the synthesized click; `ProductQuickView.client.tsx:168-186` card activation guarded only against `.pc-arrow`). Faithful to the mobile prototype, which has the same lack of tap/swipe discrimination. Reachable now on touch devices but browser-dependent (synthesized-click heuristics after a horizontal drag), so it needs a real-device check plus a shared "moved past threshold" flag on `.pc-media` that swallows the trailing `click`; the fix cross-cuts the 5.2 slider and 5.4 activation, so it belongs to a focused touch-UX pass rather than an unattended patch on either island alone.

- source_spec: `spec-5-4-product-quick-view.md`
  summary: `document.body.style.overflow='hidden'` does not lock the background behind the fixed mobile bottom-sheet on iOS Safari — the page still scrolls under touch behind the open `.pd-sheet` (the whole mobile composition).
  evidence: Well-known iOS behavior (`overflow:hidden` on `<body>` is ignored for `position:fixed` overlays); faithful to the prototype's identical `body{overflow:hidden}` lock, so not a regression, but real on the primary mobile browser. The robust fix (position:fixed body with scroll-offset save/restore, or `overscroll-behavior:contain` + touch-move prevention on the sheet) is heavier than an unattended patch and should land once as a shared mobile scroll-lock utility — it also applies to the Story 1.5 drawer scroll-lock, so a single shared approach is preferable to a per-overlay half-measure.

- source_spec: `spec-5-4-product-quick-view.md`
  summary: `ProductQuickView` assumes the static 12-product `PRODUCTS` shape and is not hardened for dynamic/CMS data — empty `imgs` renders "View 1 of 0" and empty `specs`/`fits`/`offers` render orphan section headers (and the disclaimer) above nothing; value-derived React keys (`key={f}` fits, `key={k}` specs, `key={`${brand} ${name}`}` cards/grid) collide on duplicate values, a duplicate `${brand} ${name}` makes the second product unreachable via `details.find` (first-match) and collides its `data-pd-key`, and `ProductDetail` hand-mirrors 9 `Product` fields so a future `Product` field rename silently drops from the quick-view.
  evidence: All latent — the current static `PRODUCTS` always has ≥2 imgs, non-empty specs/fits, `buildOffers` always returns 2–3 offers, and every `${brand} ${name}` is unique across all 12 products, so none of these trigger today (both reviewers independently confirmed key integrity and non-emptiness for the current data). They become real when the module's flagged future Payload/CMS collection replaces the static array (CMS is on the roadmap). Cheap, coherent hardening for that seam: length-guard each header+list, use index-composite keys (`${k}-${i}`), derive/enforce an intrinsically unique product key (sku or category+index), and `type ProductDetail = Pick<Product, 'brand'|'domain'|'name'|'imgs'|'rating'|'reviews'|'specs'|'fits'|'desc'> & { key: string; offers: Offer[] }`. Deferred because it has no present consumer and would add defensive churn to a green static-data story.

- source_spec: `spec-5-4-product-quick-view.md`
  summary: The offer disclaimer text is hand-duplicated as the island's `DISCLAIMER` const — a verbatim copy of `OFFER_DISCLAIMER` in `src/lib/offers.ts` kept in sync only by hand — so a future one-sided edit ships two divergent legal disclaimers (product-card/offer context vs quick-view).
  evidence: The duplication is a deliberate, spec-documented choice to satisfy AD-9 (the island must not import the server-only `lib/offers.ts` runtime) and both copies are currently identical, so it is not a present defect. The clean, AD-9-safe resolution is to hoist the string into a dependency-free pure-constant module that both the server module and the island import; deferred as low-risk maintainability tech-debt that pairs naturally with the existing Story 5.3 `import 'server-only'` boundary-hardening deferral.

- source_spec: `spec-5-5-brand-marquee.md`
  summary: Marquee favicons reuse `FaviconImg`, which HIDES a failed favicon (`visibility:hidden`) instead of the prototype's `this.remove()`, and has no `naturalWidth<24` placeholder check — so a missing or Google-generic-globe favicon leaves a permanent 34px+12px empty gutter in the tile (the prototype collapses it), a pixel-fidelity gap on failure.
  evidence: Latent for the current static 14+3 well-known brand domains (their Google favicons resolve, so no gap shows today), which is why it was not patched. It becomes visible if a brand domain's favicon 404s or returns the generic globe. The spec deliberately chose `FaviconImg` reuse (Design Notes) and it satisfies the epic empty-state rule ("onerror прячет img, верстка не рушится"), so this is a fidelity refinement, not a spec deviation. Clean fix: give `FaviconImg` a "collapse on failure" mode (`display:none` + an `onLoad` guard replicating `naturalWidth<24`) for marquee use, without regressing the card/chip call sites.

- source_spec: `spec-5-5-brand-marquee.md`
  summary: The desktop fly-to-center spotlight has no dialog semantics (no `role="dialog"`/`aria-modal`, no focus move/trap/restore) and Automotive `.linkless` tiles are non-focusable `<div>`s bound to click only, so the spotlight is mouse-only and keyboard/AT users cannot reach the "Trusted partner brand" card.
  evidence: Real for keyboard/screen-reader users, but out of current scope by design: the spotlight is a decorative flourish ported VERBATIM from the prototype (click-only, no ARIA), and the epic's Accessibility Floor is explicitly basic ("не полный WCAG AA") with "при конфликте a11y с отрисованным пикселем — пиксель приоритетнее". Health tiles remain real focusable `<a href>` (Enter navigates to the brand site — the actionable path), and the Automotive card exposes no actionable content, so keyboard users lose nothing actionable. Deferred for a future focused a11y pass that could add `role="button"`/`tabindex`/keydown activation and dialog focus management to the spotlight without changing the rendered pixel.

- source_spec: `spec-5-5-brand-marquee.md`
  summary: Under `prefers-reduced-motion` the marquee `animation:none` pins the double-width `max-content` track at `translateX(0)`, so only the first screenful of brands is visible and the rest are clipped off-screen with no scroll or controls to reach them — reduced-motion users lose brand content.
  evidence: Verbatim prototype behavior and exactly what the spec/epic mandate ("prefers-reduced-motion выключает авто-скролл marquee"), so it is not a deviation, but it is a genuine content-reachability gap for RM users. A considerate fix (out of the verbatim-port scope): under RM, drop the `expand()` duplication / `max-content` and make the row wrap or horizontally scroll so every brand is reachable statically. Deferred as an RM-UX enhancement.

- source_spec: `spec-5-5-brand-marquee.md`
  summary: `BrandSpotlight` binds handlers to a one-time `querySelectorAll('.catalog-dk .logo-tile')` snapshot (empty-deps effect) and the seamless loop assumes `autoRepeat`/`healthRepeat` are even and ≥2; both are correct for the static content but brittle if brands become dynamic/CMS-driven (new tiles get no handlers; an odd/1/0 repeat lands `translateX(-50%)` mid-copy → visible seam, or an empty track).
  evidence: No present consumer — the 14+3 brands and `autoRepeat:2`/`healthRepeat:8` are fixed and correct, and the marquee loops seamlessly today (both reviewers noted the snapshot/leak concerns are benign under static content with thorough effect cleanup). Pairs with the same Phase-2 Payload seam flagged for Stories 5.2–5.4: when `catalogContent.brands` moves to CMS, re-query on content change (or key the effect on the brand set) and enforce/normalize an even repeat (`Math.max(2, repeat + (repeat % 2))`). Deferred as dynamic-data hardening with no static-content trigger.

- source_spec: `spec-6-3-contact-inline-form-map-location-tabs.md`
  summary: On mobile, `/contact` renders the reused Epic 2 `ContactInline` as a single collapsed `.contact-card`, whereas `Contact Mobile.html` splits it into two sections — a light form section (`.section.bg > .wrap`) and a full-bleed dark info panel (`.section`, `#2f2f2f`) — so the mobile pixel does not match the prototype section-for-section.
  evidence: Real mobile-fidelity gap, but dictated by Story 6.3's mandate to mount `ContactInline` exactly once and NOT rebuild the form (epic-6-context: "форма приходит готовым компонентом из Epic 2... Никаких новых форм здесь не создаётся"). The single-composition `.contact-card` is Epic 2's own design (Story 2.4); it collapses one-column <768px via components.css. Closing the gap means either splitting the card into two mobile sections (a `ContactInline` rebuild that belongs to a focused Epic 2 mobile-fidelity pass) or a dk/mb dual-composition of the form — which would double-mount `id="contactForm"` (already flagged in this file). Deferred so the fix lands where the form contract lives, verified against all three display modes.

- source_spec: `spec-6-3-contact-inline-form-map-location-tabs.md`
  summary: `.reveal` sections that live inside the hidden dk/mb composition never receive `.in` (RevealOnScroll's IntersectionObserver observes both compositions at mount, but the `display:none` one never intersects), so after a viewport resize CROSSING the 768px breakpoint mid-session the newly-shown composition's below-fold sections can stay stuck at `opacity:0`.
  evidence: Real and confirmed by two independent reviewers, but pre-existing and SYSTEMIC to the whole site's AD-3 dk/mb + `.reveal` pattern (every page — brands/shops/about/catalog — carries `.reveal` on both compositions), not caused by Story 6.3. Low real-user frequency (a real visitor keeps one device width; the trigger is window-resize across 768px or responsive DevTools). The fix belongs in `RevealOnScroll.client.tsx` (re-observe on breakpoint change, or force-`.in` the shown composition on media change) as a focused sitewide pass, not a per-story patch.

- source_spec: `spec-6-3-contact-inline-form-map-location-tabs.md`
  summary: The Contact map location switcher (`MapLocations.client.tsx`) uses plain `<button>`s with no tab-widget semantics (`role="tablist"`/`role="tab"`/`aria-selected`), no roving-tabindex arrow-key navigation, and no live-region announcement when the map iframe `src` changes — so assistive-tech users cannot perceive which location is active or that clicking drives the map.
  evidence: Real a11y gap, but a verbatim port of the prototype (Contact.html uses plain buttons + `getElementById`), and consistent with the project's established pattern of deferring a11y-beyond-prototype to a focused pass (see the header reduced-motion, drawer focus-trap, ContactForm error/success-announcement, and skip-link deferrals in this file). The correct ARIA pattern is also non-obvious (the iframe is not a true tabpanel — these are toggle buttons driving one shared surface), so wrong ARIA could regress rather than help; warrants deliberate SR verification rather than an unattended patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-company-passport-sitesettings.md`
  summary: Десктопные пины US-presence карты в `src/content/about.ts` (Story 4.2, D3-карта) держат паспорт-смежные литералы (city/state «Sheridan/WY», «Houston/TX», Entity «Rollun LC», summary-часы «Mon–Fri 9–6») — второй дом мимо `SiteSettings`; правка адреса/компании в админке разойдётся между десктопными пинами и мобильными карточками.
  evidence: Ревью (Edge Case Hunter) на диффе 7.1. Вне scope 7.1 (AC4 = Epic 1/6; пины — контент страницы About). Логично к Story 7.3 (Page Globals), когда контент About переезжает в `AboutContent`-Global. Summary-часы «Mon–Fri 9–6» не совпадают ни с одним реальным паспорт-атомом (это карт-презентация), что подтверждает: это не канонический атом, а данные карты.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-company-passport-sitesettings.md`
  summary: `map.initialSrc` (iframe Google Maps) в `src/content/contact.ts` зашивает адрес магазина литералом (`53%2F27…Houston…77039`) — дубликат паспорт-значения `shopAddress`, который не отследит правку в админке (на первом рендере карта покажет старый адрес до клика по табу).
  evidence: Ревью (Edge Case Hunter) на диффе 7.1. Намеренное AD-13-поведение (typo-then-correct) сохранено; сама дупликация адреса — второй дом. Вне scope 7.1 (контент карты страницы Contact → Story 7.3 Page Globals). Требует композиции `initialSrc` из `shopAddress` с сохранением namerennoгo `53%2F27`-typo.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-media-collection-image-optimization.md`
  summary: Под `output:'standalone'` (Docker) `Media.upload.staticDir='public/media'` пишет на эфемерный контейнерный диск — без персистентного volume или cloud storage-адаптера (`@payloadcms/storage-*`) все загруженные менеджером картинки 404-ят после каждого редеплоя/рестарта.
  evidence: Ревью (Edge Case Hunter) на диффе 7.2. Реально для прод-деплоя; фикс — инфраструктурный (volume-маунт на `/app/public/media` ИЛИ storage-адаптер S3/R2), решение о деплое, не unattended code-патч. Пересекается со Story 1.1 (deploy) и контуром ревалидации 7.4. Локально/в dev не проявляется (файлы на диске проекта).

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-media-collection-image-optimization.md`
  summary: `next.config images.localPatterns:[{pathname:'/api/media/**'}]` работает, пока `Media.url` относительный; если Payload `serverURL`/`PAYLOAD_PUBLIC_SERVER_URL` будет задан (частая прод-настройка), `url` станет абсолютным → `next/image` отклонит незаданный хост (400), и понадобится `images.remotePatterns` под этот хост.
  evidence: Ревью (Edge Case Hunter) на диффе 7.2. Сейчас `serverURL` не задан — проверено: media отдаётся `/_next/image` 200 webp по относительному пути. Латентная хрупкость, срабатывает при будущей смене конфигурации домена; логично закрыть вместе с деплой-настройкой (Story 1.1 / прод-конфиг).

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-on-demand-content-revalidation.md`
  summary: Media collection has no afterChange revalidation hook — editing an existing Media document in place (replacing/re-cropping the file) does not fire any Global's afterChange, so pages that populate that Media via a Global image slot (e.g. About ceoPhoto, depth:2) keep serving the stale image URL until an unrelated Global save purges the tag.
  evidence: Story 7.4 deliberately scoped revalidation to the seven Globals + passport (spec Never: "не трогать Media"; epic AD-10 is Global-tag-centric). The primary image-change workflow (upload new Media, assign to a Global slot) DOES revalidate via the Global hook; only in-place edits of an already-referenced Media doc are missed. A focused follow-up should add a Media afterChange that revalidates the tags of every Global referencing it (or a shared media tag).

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-products-brands-shops-roles.md`
  summary: `Users.role` defaults to `admin`; a user created without an explicit role silently becomes an admin.
  evidence: Least-privilege would default to `manager`, but the current access matrix needs the first bootstrap user to be admin. Only admins can create users, so blast radius is small, yet an admin who forgets the role field creates an accidental admin. Revisit when the permission matrix (marked `[ASSUMPTION]` in Epic 8) is finalized — e.g. default `manager` + explicit first-user bootstrap.
- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-products-brands-shops-roles.md`
  summary: Brands marquee link/linkless rendering keys off `href` presence independently of the `line` field, so a manager can create an inconsistent tile (auto-with-href or health-without-href).
  evidence: `toBrand` spreads `href` on presence and `LogoTile` renders `<a>` vs `.linkless <div>` off `href` alone; the `line` field does not constrain it. Seeded data is correct, so no current regression, but editor discipline is unguarded. Consider deriving linkage from `line` or adding a validation hook.
